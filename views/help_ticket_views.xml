<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!--  Ticket form view-->
    <record id="help_ticket_view_form" model="ir.ui.view">
        <field name="name">help.ticket.view.form</field>
        <field name="model">help.ticket</field>
        <field name="arch" type="xml">
            <form string="Ticket" class="o_event_form_view">
                <header>
                    <button name="action_send_reply" string="Reply"
                            type="object" class="btn-primary"/>
                    <button name="action_create_tasks" string="Create Task"
                            type="object" class="btn-primary"
                            invisible="[('create_task','=',False)]"/>
                    <button name="action_create_invoice" string="Create Invoice"
                            type="object" class="btn-primary"
                            invisible="[('billable','=',False)]"/>
                    <field name="stage_id" widget="statusbar"
                           options="{'clickable': '1'}"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button class="oe_stat_button" name="action_open_tasks"
                                string="Tasks" type="object" icon="fa-tasks"
                                invisible="[('create_task','=',False)]">
                        </button>
                        <button class="oe_stat_button"
                                name="action_open_merged_tickets"
                                string="Merged Tickets" type="object"
                                icon="fa-list"
                                invisible="[('merge_ticket_invisible','=',False)]">
                            <div class="o_stat_info">
                                <field name="merge_count" readonly="True"/>
                                <span class="o_stat_text">Merged Tickets</span>
                            </div>
                        </button>
                        <button class="oe_stat_button" name="action_open_invoices"
                                string="Invoices" type="object" icon="fa-tasks"
                                invisible="[('billable','=',False)]">
                        </button>
                    </div>
                    <field name="kanban_state" widget="state_selection"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name"/>
                        </h1>
                        <label for="subject"/>
                        <h1 class="mt0">
                            <field name="subject"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="customer_id"/>
                            <field name="ticket_type"/>
                            <field name="email"/>
                            <field name="phone"/>
                            <field name="tags" widget="many2many_tags"/>
                        </group>
                        <group>
                            <field name="show_category" invisible="1"/>
                            <field name="priority" widget="priority"/>
                            <field name="create_date"/>
                            <field name="start_date"/>
                            <field name="end_date"/>
                            <field name="product_ids" widget="many2many_tags"/>
                            <field name="merge_count" invisible="1"/>
                            <field name="merge_ticket_invisible" invisible="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Issue Description">
                            <field name="description"
                                   placeholder="Provide the reason in detail"/>
                        </page>
                        <page string="Attachments">
                            <field name="attachment_ids">
                                <list string="Attachments" no_open="1">
                                    <field name="name"/>
                                    <field name="res_model"/>
                                    <field name="res_id"/>
                                    <field name="type"/>
                                    <field name="file_size"/>
                                    <field name="create_uid"/>
                                    <field name="create_date"/>
                                    <field name="datas" widget="binary"
                                           filename="name"/>
                                </list>
                            </field>
                        </page>
                        <page string="Customer Rating">
                            <group>
                                <field name="customer_rating"
                                       widget="priority"/>
                                <field name="review"/>
                            </group>
                        </page>
                        <page string="Other Information">
                            <group>
                                <group>
                                    <field name="team_id"/>
                                    <field name="team_head"/>
                                    <field name="assigned_user"/>
                                    <field name="time"/>
                                    <field name="category_id"/>
                                </group>
                                <group>
                                    <field name="project_id"/>
                                    <field name="service_product_id" invisible="[('create_task','=',False)]" required="[('create_task','=', True)]"/>
                                    <field name="show_create_task" invisible="1"/>
                                    <field name="create_task" invisible="[('show_create_task','=',False)]"/>
                                    <field name="billable" invisible="[('create_task','=',False)]"/>
                                    <field name="last_update_date"/>
                                    <field name="replied_date"/>
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>
    <!--Ticket list view-->
    <record id="help_ticket_view_tree" model="ir.ui.view">
        <field name="name">help.ticket.view.tree</field>
        <field name="model">help.ticket</field>
        <field name="arch" type="xml">
            <list default_order="name desc">
                <field name="name"/>
                <field name="customer_id"/>
                <field name="assigned_user" string="Assigned User"/>
                <field name="subject"/>
                <field name="stage_id"/>
            </list>
        </field>
    </record>
<!--    Ticket quick create form view-->
    <record id="help_ticket_view_form_quick_create" model="ir.ui.view">
        <field name="name">help.ticket.view.form.quick.create</field>
        <field name="model">help.ticket</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <field name="subject" string="Subject"/>
                    <field name="customer_id" string="Customer"/>
                    <field name="description" string="Description"/>
                </group>
            </form>
        </field>
    </record>
    <!--   Ticket kanban view-->
    <record id="help_ticket_view_kanban" model="ir.ui.view">
    <field name="name">help.ticket.view.kanban</field>
    <field name="model">help.ticket</field>
    <field name="arch" type="xml">
        <kanban default_group_by="stage_id"
                class="o_kanban_small_column"
                quick_create="true"
                quick_create_view="odoo_website_helpdesk.help_ticket_view_form_quick_create"
                on_create="quick_create">

            <field name="color"/>
            <field name="name"/>
            <field name="stage_id"/>
            <field name="kanban_state"/>
            <field name="customer_id"/>
            <field name="tags"/>
            <field name="team_id"/>
            <field name="priority"/>
            <field name="activity_ids"/>

            <progressbar field="kanban_state"
                         colors='{"done": "success", "blocked": "danger", "normal": "200"}'/>

            <templates>
                <t t-name="card">
                    <div class="oe_kanban_global_click o_kanban_card">
                        <div>
                            <strong>
                                <field name="name"/>
                            </strong>
                        </div>

                        <div>
                            <field name="customer_id"/>
                        </div>

                        <div>
                            <field name="tags" widget="many2many_tags"/>
                        </div>

                        <div t-if="record.team_id.raw_value">
                            <field name="team_id"/>
                        </div>

                        <footer>
                            <field name="priority" widget="priority"/>
                            <field name="kanban_state" widget="state_selection"/>
                            <field name="activity_ids" widget="kanban_activity"/>
                        </footer>
                    </div>
                </t>
            </templates>
        </kanban>
    </field>
</record>
    <!--Ticket Calendar view-->
    <record id="help_ticket_view_calendar" model="ir.ui.view">
        <field name="name">help.ticket.view.calendar</field>
        <field name="model">help.ticket</field>
        <field name="arch" type="xml">
            <calendar string="Helpdesk Ticket" date_start="create_date"
                      mode="month" color="customer_id">
                <field name="customer_id"/>
                <field name="subject"/>
                <field name="team_id"/>
            </calendar>
        </field>
    </record>
    <!--Ticket pivot view-->
    <record id="help_ticket_view_pivot" model="ir.ui.view">
        <field name="name">help.ticket.view.pivot</field>
        <field name="model">help.ticket</field>
        <field name="arch" type="xml">
            <pivot>
                <field name="customer_id" type="col"/>
                <field name="subject" type="row"/>
                <field name="team_id" type="col"/>
            </pivot>
        </field>
    </record>
    <!--Ticket graph view-->
    <record id="help_ticket_view_graph" model="ir.ui.view">
        <field name="name">help.ticket.view.graph</field>
        <field name="model">help.ticket</field>
        <field name="arch" type="xml">
            <graph>
                <field name="customer_id"/>
                <field name="team_id"/>
                <field name="stage_id"/>
            </graph>
        </field>
    </record>
    <!--Ticket search view-->
    <record id="help_ticket_view_search" model="ir.ui.view">
        <field name="name">help.ticket.view.search</field>
        <field name="model">help.ticket</field>
        <field name="arch" type="xml">
            <search string="Ticket Search">
                <field name="name" string="Ticket"/>
                <field name="subject"/>
                <filter string="My Ticket" name="my_ticket"
                        domain="[('assigned_user', '=', uid)]"/>
                <filter string="Archived" name="filter_archived"
                        domain="[('active','=', False)]"/>
                <filter string="UnArchived" name="filter_unarchived"
                        domain="[('active','=', True)]"/>
                <separator/>
                <filter string="Customer" name="group_customer"
                        context="{'group_by':'customer_id'}"/>
                <filter name="group_team" string="Team"
                        context="{'group_by':'team_id'}"/>
                <filter name="group_assign" string="Assigned To"
                        context="{'group_by':'assigned_user'}"/>
            </search>
        </field>
    </record>
<!--    Ticket view action-->
    <record id="help_ticket_action" model="ir.actions.act_window">
        <field name="name">Helpdesk Tickets</field>
        <field name="res_model">help.ticket</field>
        <field name="view_mode">kanban,list,form,calendar,pivot,graph</field>
        <field name="view_id" ref="help_ticket_view_kanban"/>
        <field name="search_view_id" ref="help_ticket_view_search"/>
        <field name="views" eval="[
            (ref('help_ticket_view_kanban'), 'kanban'),
            (ref('help_ticket_view_tree'), 'list'),
            (ref('help_ticket_view_form'), 'form'),
            (ref('help_ticket_view_calendar'), 'calendar'),
            (ref('help_ticket_view_pivot'), 'pivot'),
            (ref('help_ticket_view_graph'), 'graph')
        ]"/>
        <field name="target">current</field>
        <field name="context">{}</field>
    </record>
<!--    My ticket view action-->
    <record id="help_ticket_my_ticket_action" model="ir.actions.act_window">
        <field name="name">My Helpdesk Tickets</field>
        <field name="res_model">help.ticket</field>
        <field name="domain">['|', ('assigned_user','=',uid), ('team_head','=', uid)]</field>
        <field name="view_mode">kanban,list,form,calendar,pivot,graph</field>
        <field name="view_id" ref="help_ticket_view_kanban"/>
        <field name="search_view_id" ref="help_ticket_view_search"/>
        <field name="views" eval="[
            (ref('help_ticket_view_kanban'), 'kanban'),
            (ref('help_ticket_view_tree'), 'list'),
            (ref('help_ticket_view_form'), 'form'),
            (ref('help_ticket_view_calendar'), 'calendar'),
            (ref('help_ticket_view_pivot'), 'pivot'),
            (ref('help_ticket_view_graph'), 'graph')
        ]"/>
        <field name="target">current</field>
        <field name="context">{}</field>
    </record>
<!--    Ticket report action-->
    <record id="help_ticket_report_action" model="ir.actions.act_window">
        <field name="name">Report Help Desk</field>
        <field name="res_model">help.ticket</field>
        <field name="view_mode">graph,search</field>
        <field name="view_id" ref="help_ticket_view_graph"/>
        <field name="search_view_id" ref="help_ticket_view_search"/>
        <field name="target">current</field>
    </record>

</odoo>
